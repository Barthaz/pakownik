BETA_MODE=false
# Na Vercel ustaw dokładnie "false" (małe litery). Brak zmiennej = tryb beta (plik JSON).
JWT_SECRET=change-me-in-production
PORT=3001
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=pakownik
# Po ustawieniu DB_* uruchom lokalnie: npm run db:migrate && npm run db:seed
